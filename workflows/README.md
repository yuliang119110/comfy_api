Stored custom ComfyUI workflow JSON records will be persisted here by `/api/workflows`.
This directory is intentionally separate from `templates/`.

- `templates/`: standard main-flow gateway templates
- `workflows/`: user-managed independent custom workflows
