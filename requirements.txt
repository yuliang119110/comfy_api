# ComfyUI API Gateway - Standalone Dependencies
# This service is a lightweight HTTP/WS proxy and does NOT require
# torch, transformers, or any ComfyUI internals. It communicates
# with ComfyUI instances purely via HTTP API and WebSocket.

# --- Core Framework ---
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0,<3.0.0

# --- HTTP & WebSocket ---
httpx>=0.25.0
websockets>=11.0

# --- Config & Parsing ---
pyyaml>=6.0

# --- File Upload Support ---
python-multipart>=0.0.6

# --- MCP (Model Context Protocol) ---
# Optional: only needed if you want to expose generation as MCP tools
# Install separately if needed: pip install mcp>=1.0.0
# mcp>=1.0.0
